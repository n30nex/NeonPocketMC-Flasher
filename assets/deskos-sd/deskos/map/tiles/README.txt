DeskOS cached and preloaded map tiles live below this directory.

The built-in interactive OpenStreetMap Standard cache uses:
  openstreetmap/z{z}/x{x}/y{y}.png

Do not bulk-download from tile.openstreetmap.org. A separately supplied tile
set may be imported only when its provider manifest explicitly confirms that
offline storage/prefetch is permitted.
